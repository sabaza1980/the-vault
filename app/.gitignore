# Environment variables — never commit these
.env
.env.local
.env.*.local
.env.*
.env.production
.env.staging

# Admin panel local config (contains Firebase + Loops keys — never commit)
public/admin-config.js

# Firebase Android config — contains API key; restrict key in Google Cloud Console
google-services.json

# Capacitor native projects (large; add/remove per your workflow)
ios/
android/

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

.vercel
.env*.local
